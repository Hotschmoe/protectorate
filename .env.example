# Protectorate Environment Configuration
# Copy to .env and customize as needed

# =============================================================================
# Required for Production
# =============================================================================

# Claude OAuth token (generated via: claude setup-token)
# This is a long-lived token for container authentication
CLAUDE_CODE_OAUTH_TOKEN=

# =============================================================================
# Host Paths (auto-detected by installer, rarely need to change)
# =============================================================================

# Where sleeve workspaces are stored on host
WORKSPACE_HOST_ROOT=${PWD}/workspaces

# Claude credentials location on host (for credential fallback)
CREDENTIALS_HOST_PATH=${HOME}/.claude/.credentials.json
SETTINGS_HOST_PATH=${HOME}/.claude.json
PLUGINS_HOST_PATH=${HOME}/.claude/plugins

# =============================================================================
# Development Overrides
# =============================================================================

# Override sleeve image for local development
# SLEEVE_IMAGE=protectorate-sleeve:latest

# Enable development mode (more verbose logging)
# DEV_MODE=true

# =============================================================================
# Optional API Keys
# =============================================================================

# Anthropic API key (for direct API access, separate from Claude subscription)
# ANTHROPIC_API_KEY=

# OpenAI API key (for future multi-model support)
# OPENAI_API_KEY=

# Google AI API key (for Gemini CLI support)
# GOOGLE_AI_API_KEY=
